

import 'package:flutter/material.dart';

class Communautes extends StatelessWidget {
  const Communautes({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Commés'),
      ),
      body: Center(
        child: Text('Actualités'),
      ),
    );
  }
}
